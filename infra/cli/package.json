{
  "name": "@frak-sandbox/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/frak-sandbox",
    "build:linux": "bun build src/index.ts --compile --target=bun-linux-x64 --outfile dist/frak-sandbox-linux-x64"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
