# Minimal chrony config for Firecracker microVMs
server time1.google.com iburst
server time2.google.com iburst
pool pool.ntp.org iburst maxsources 2

# Step clock if offset > 1s, on any update (no gradual slew for large offsets)
makestep 1.0 -1

driftfile /var/lib/chrony/drift
port 0
rtcsync
