{
  "name": "@atelier/compose",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The one shared client-side SDK: harness composers, presets, spec merge, sync manifests. Composition happens in the client (atelier-v2 §3).",
  "exports": {
    ".": "./src/index.ts",
    "./harnesses/opencode": "./src/harnesses/opencode.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@atelier/spec": "workspace:*",
    "@frak/atelier-shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  }
}
