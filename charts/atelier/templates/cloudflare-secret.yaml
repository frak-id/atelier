{{- if and .Values.certManager.enabled .Values.certManager.cloudflare.apiToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.certManager.cloudflare.apiTokenSecretRef.name }}
  namespace: {{ .Values.certManager.namespace | default "cert-manager" }}
  labels:
    {{- include "atelier.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.certManager.cloudflare.apiTokenSecretRef.key }}: {{ .Values.certManager.cloudflare.apiToken | b64enc | quote }}
{{- end }}
